// Rick's robotics school

const walking = 2.06

const distance = 6.250

const biking = distance - walking

console.log(`The distance is ${distance}km, and he walked ${walking}km so that means he biked ${biking}km.`)

// Sana 15 laps

const laps = 15

const field = 22+35+40

const totalDistance = laps * field

console.log(`Sana ran ${laps} laps around the field, and the fields triangular side lengths are 22m, 35m, and 40m. In total, the field is ${field}m so she ran ${totalDistance}m.`)